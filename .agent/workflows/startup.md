---
description: Start the VisaEvidence UK development server
---

// turbo-all
1. Ensure dependencies are installed
```powershell
npm install
```

2. Start the Expo server in web mode
```powershell
npx expo start --web
```
